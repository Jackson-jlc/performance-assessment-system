# 经营考核系统 · 自用版

钻井经营考核系统封装后的本地自用软件。**双击即用，全部数据保存在本机，不依赖外网、不上传任何数据。**

---

## 一、怎么启动

**推荐方式：双击 `启动经营考核系统.vbs`**

- 服务会在后台静默启动（无黑窗口），并自动打开浏览器进入系统；
- 服务一直在后台运行，可以随时关闭浏览器，数据照常保存；
- 如果服务已经启动，再次双击会直接打开浏览器，不会重复启动。

**调试方式：双击 `启动经营考核系统（调试模式）.bat`**

- 前台运行，日志实时显示在窗口里，关闭窗口即停止服务；
- 一般只在排错、或想直接看启动日志时使用。

> 需要电脑上已安装 Node.js 18 或更高版本（本机已装好，直接可用）。
> 换到别的电脑时，如果提示"未检测到 Node.js"，先到 https://nodejs.org/zh-cn/download 安装。

## 二、第一次使用：获取登录账号

第一次启动时系统会自动创建管理员账号，密码是**随机生成**的，打印在启动日志里：

1. 启动系统后，双击 `查看运行日志.bat`（用记事本打开 `logs\server.log`）；
2. 在日志开头找到这一段：
   ```
   [auth] 已创建初始管理员账号，请立即登录后在"账号管理"里修改密码：
           用户名: admin
           密码  : xxxxxxxx
   ```
3. 用这个账号登录系统，登录后进入「账号管理」修改密码，并可创建其他同事的账号（editor 角色）。

> 只有第一次启动（本机还没有账号数据）才会打印初始密码，之后不会再出现。

## 三、数据存在哪里（重点）

**全部数据都在本软件目录内，即 `server\data` 文件夹**，是纯本地 JSON 文件：

| 文件 | 内容 |
|---|---|
| `wells.json` | 井库（完成井考核库 + 一井一策样本） |
| `quota.json` | 定额库（附件1 基线 + 滚动定额） |
| `filings-2026.json` | 年度月度填报数据（切块考核） |
| `teaminfo.json` | 队伍信息月度台账（队号 × 年月，首次登录自动建账并补全队伍名单） |
| `users.json` | 账号信息（密码为加密存储） |

特点：

- **完全离线可用**：所有功能不联网，数据不出本机；
- **不会丢**：数据实时落盘（先写临时文件再原子替换，断电也不会半写损坏）；
- **不传云端**：系统没有向任何外部服务器上传数据的逻辑。

## 四、备份与恢复

- **一键备份**：双击 `数据备份.bat`，会把全部数据复制到 `备份\data-日期时间\` 文件夹；
- 把整个 `备份` 文件夹拷到 U 盘 / 网盘 / 另一台电脑，就是完整备份；
- **恢复**：把备份文件夹里的 json 文件复制回 `server\data` 目录（覆盖同名文件），再启动系统即可，账号、井库、填报数据全部原样回来。

建议：每周至少备份一次；涉及重要考核数据前后各备份一次。

## 五、停止服务

双击 `停止经营考核系统.bat`，会结束后台服务（数据已全部落盘，不会丢失）。

> 注意：`启动经营考核系统.vbs` 启动的是后台服务，电脑重启后不会自动运行，需要再次双击启动。如果希望开机自启，可以把 `启动经营考核系统.vbs` 的快捷方式放进"启动"文件夹（Win+R 输入 `shell:startup` 回车即可打开）。

## 六、常见问题

**1. 端口被占用 / 想换端口**

默认端口 4000。若 4000 被别的程序占用：
- 用记事本打开 `server\.env`，把 `PORT=4000` 改成其它端口（如 `PORT=4100`）；
- 保存后重启系统。启动器会自动读取 .env 里的端口。

**2. 忘了密码**

账号数据在 `server\data\users.json`。若所有账号密码都忘了：
- 停止服务 → 把 `server\data\users.json` 改名或移走（备份一下）→ 重新启动；
- 系统会重新创建管理员账号，新密码见 `logs\server.log` 开头（或调试模式窗口）。

**3. 前端页面升级**

新版 `index.html` 直接覆盖 `server\public\index.html` 即可，数据和账号不受影响。

**4. 换电脑迁移**

整份复制本文件夹（含 `server\data`）到新电脑，装好 Node.js 后双击启动即可。

## 七、目录结构

```
经营考核系统自用版/
├── 启动经营考核系统.vbs            ← 日常双击这个启动
├── 启动经营考核系统（调试模式）.bat ← 前台运行，看日志用
├── 停止经营考核系统.bat
├── 数据备份.bat
├── 查看运行日志.bat
├── 使用说明.md
├── logs/                           ← 运行日志（含首次启动的初始密码）
├── 备份/                           ← 数据备份输出目录
└── server/
    ├── server.js                   ← 服务入口
    ├── public/index.html           ← 系统前端页面
    ├── lib/                        ← 存储/认证
    └── data/                       ← ★ 全部业务数据（本地）
```
